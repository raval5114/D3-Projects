import 'package:flutter/material.dart';

class JourneyHistory extends StatelessWidget {
  const JourneyHistory({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Journey History"));
  }
}
